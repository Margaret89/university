extends tmpl/_layout

block vars
	- var pageId = 'main'

block sidebar
	include ./tmpl/_sidebar-main

block body
	section.page-content
		h2 Список всех страниц
		if pages
			ul
				each page in pages
					li
						a(href=page.href, target='_blank')= page.title