extends tmpl/_layout

block vars
	- var pageId = 'main'

block sidebar
	|sidebar

block body
	section Список всех страниц
		if pages
			ul
				each page in pages
					li
						a(href=page.href, target='_blank')= page.title